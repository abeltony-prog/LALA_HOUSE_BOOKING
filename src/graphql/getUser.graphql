query getUsers($email: String = "") {
  users(where: {email: {_eq: $email}}) {
    UID
    email
    gender
    name
    password
    role
    hosts {
      HID
      join_on
      name
      user_id
    }
  }
}
